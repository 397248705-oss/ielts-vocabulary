# 雅思单词本

手机浏览器里的个人雅思背单词 PWA。

## 本地运行

```powershell
npm install
npm run dev
```

打开本地地址后，可以在同一局域网的手机浏览器中访问。

## 验证

```powershell
npm test
npm run build
npm run e2e
```

## 数据

学习记录只保存在浏览器本地。设置页支持导出和导入 JSON 备份。
